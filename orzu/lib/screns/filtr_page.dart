import 'package:flutter/material.dart';

class FiltirPage extends StatelessWidget {
  const FiltirPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("filtr")),);
  }
}